# Private secrets — contains live API keys, must never be committed
shell/.zsh/secrets.zsh
# Personal shell history
shell/.zsh/.zsh_history
# Nix build artifact
home-manager/result
# Nix build output symlink (running nix build from repo root)
/result
